<p id="notice"><%= notice %></p>

<% @posts.each do |post| %>
  <div>
    <h3 class="post-title"><%= link_to "#{post.title}", post %></h3>
    <p><%= post.body.split[0..80].join(' ').gsub(/[^0-9a-z \.]/i, '') %></p>
  </div>
<% end %>
<br>

<%= link_to 'New Post', new_post_path %>
